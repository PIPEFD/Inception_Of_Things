# No editar a mano: 'make inventory' genera infra/ansible/inventory.ini
# (gitignored) con este mismo formato, rellenando la IP real desde
# 'terraform output -raw public_ip'.
#
# Solo la IP: usuario, clave e intérprete NO van aquí, están en
# group_vars/iot_host.yml (trackeado, no es secreto) y se expanden desde
# el mismo ADMIN_USER/SSH_KEY que usa el Makefile.
[iot_host]
203.0.113.10
